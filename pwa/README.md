# PWA (skeleton — built in Phase 27)

Phase 27 adds: `/manifest.webmanifest`, `/service-worker.js`, `/offline.php`,
push handling, and app icons in `assets/icons/` (maskable + any, 192px and
512px minimum). This folder holds PWA helpers if needed.
