-- =====================================================================
-- Oyejo Gas - database schema (placeholder)
-- The complete schema is created in Phase 3 (Database architecture).
-- Phase 4's installer executes this file. In production, change the
-- schema only through versioned files in database/migrations/.
-- =====================================================================
SET NAMES utf8mb4;

-- Phase 3 tables: users, roles, permissions, customers, drivers, products,
-- categories, cylinders, inventory, orders, payments, wallets, referrals,
-- spin campaigns, deliveries, pickups, refills, notifications, support
-- tickets, reports, backups, audit logs, feature toggles, add-ons.
-- (See docs/01-MASTER-REQUIREMENTS-CHECKLIST.md section C.)
SELECT 1 AS oyejo_schema_placeholder;
