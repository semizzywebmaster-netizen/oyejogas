# Example add-on

Reference implementation for the Oyejo add-on system (see
`docs/ADDON-DEVELOPMENT.md`). Install it from Admin → Add-ons to see the
full lifecycle: dependency check → migration → toggle, permission and
setting creation → enable → console menu → routed page → update.
