# Example add-on (skeleton)

Shows the manifest format only. Real add-on loading, migrations, menus and
toggles are implemented in Phase 26. This folder is blocked from direct web
access by the root `.htaccess`.
